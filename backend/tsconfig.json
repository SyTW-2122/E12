{ 
  "compilerOptions": { 
     "target": "ES2020", // Indica que queremos generar código compatible con uno de los últimos estándares de JavaScript  
     "outDir": "./dist", // Indica que el código producto de la compilación se almacenará en ./dir  
     "rootDir": "./src", // Indica que el código fuente escrito en TypeScript se encuentra en el directorio src   
     "module": "CommonJS", // Indica el estándar para cargar código desde ficheros independientes  
     "declaration": true, //Crea index.d.ts que contiene los tipos de datos que hemos usado en ts
     "noImplicitAny": true // Desactivar el uso implícito de any
     } 
  }